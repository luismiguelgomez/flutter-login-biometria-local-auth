name: flutter_application_login
description: "A new Flutter project."
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ^3.5.1

dependencies:
  flutter:
    sdk: flutter
  sqflite:
  logger: ^1.4.0
  http: ^1.2.2  # Asegúrate de usar la última versión disponible
  contact_card: ^0.0.5


  cupertino_icons: ^1.0.8
  
  # Aquí se agrega la dependencia
  local_auth: ^2.1.6

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
